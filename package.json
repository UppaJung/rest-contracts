{
  "name": "rest-contracts-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "npm run build",
    "build": "lerna run build",
    "publish-bc": "lerna publish --package rest-contracts-browser-client",
    "example": "lerna run start --scope \"rest-contracts-example\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/uppajung/rest-contracts.git"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
